# ---------- OS junk ----------
.DS_Store
Thumbs.db

# ---------- Editors ----------
.vscode/
.idea/

# ---------- Node / build outputs (root-scoped only) ----------
/node_modules/
/dist/
/build/
/.cache/
/.tmp/

# ---------- Logs & secrets ----------
*.log
.env
.env.*
!.env.example

# ---------- Netlify ----------
.netlify/

# ---------- IMPORTANT: ensure site assets under publish root are included ----------
# (Publish directory is /clients, keep everything there tracked)
!clients/**
# and specifically keep the Three/Potree libs
!clients/libs/
!clients/libs/**
!clients/EnableLogo.png
# extra safety for Three.js 'build' folder that matches the old ignore
!clients/libs/three.js/build/
!clients/libs/three.js/build/**
!clients/libs/three.js/extra/
!clients/libs/three.js/extra/**
!clients/libs/potree/
!clients/libs/potree/**
site/clients/Fida Restoration/Royal Ontario Golf Course Hole 13/Scan 0.subsampled.las
site/clients/August/tomken_shoring/hydrovault.las

# ---------- Raw scan sources (NOT served — only the converted Potree octree is) ----------
# The viewer serves pointclouds/<id>/ (cloud.js + data), never the raw LAS/E57.
*.las
*.e57
# Trimble RealWorks project DB (source for station extraction; not served).
*.scan
# Tekla IFC export (source; the served asset is the converted models/*.glb).
*.ifc

# ---------- Multi-GB feature scratch binaries (NEVER commit) ----------
# Temporal-compare / project scratch blobs (e.g. temporal_project.bin 3.6 GB,
# 7_st_dennis_project.bin 21 GB). Octree node data (data/r/*.bin) is NOT matched.
*_project.bin

# ---------- Local PotreeConverter tool checkout at repo root (not site content) ----------
/potree/

# ---------- Panorama staging dup (served copy lives in stations/panoramas/) ----------
site/clients/MurrayFranks/1531 Dupont (steel canopy)/images/

# ---------- Scratch / working files ----------
/temp work/

# ---------- Panorama staging (redundant with stations/panoramas/) ----------
# The viewer loads each station's JPG from <project>/stations/panoramas/ via the
# manifest; the flat <project>/images/ upload folder is only a staging area.
site/clients/BDA/222 Jarvis Elevator 4/images/

# ---------- HRI 40 Larch 1: panorama staging dup + local tooling scratch ----------
# HighResolution/ holds the raw-named source jpgs; served copies are in stations/panoramas/.
site/clients/HRI/**/HighResolution/
site/clients/HRI/**/_*.py
site/clients/HRI/**/feedback_*.png
site/clients/HRI/**/_stale/

# ---------- WJ Properties 7 St Dennis South Garage Slab: panorama staging dup ----------
# Served copies live in stations/panoramas/; the flat images/ folder is staging only.
# (The raw *.las and *.scan sources are already covered by the global *.las / *.scan rules.)
site/clients/WJ Properties/7 St Dennis - South Garage Slab/images/

# ---------- demo client: data served from Cloudflare R2, NOT git (2026-06-30) ----------
# demo1's octree + panoramas live in the R2 bucket 'enable-pointclouds'; projects.js
# points its folder at the r2.dev URL. Kept locally for re-testing but never committed.
# This is the pilot of the R2 migration (octrees off git -> repo stays small).
site/clients/demo/demo1/

# ---------- WJ/Solar/HRI octrees+panoramas+models served from Cloudflare R2 (2026-06-30) ----------
# All these live in the R2 bucket 'enable-pointclouds' (projects.js folders point at the
# r2.dev URL). Kept locally for re-testing but never committed. See reference_r2_migration.
site/clients/WJ Properties/7 St Dennis - South Garage Slab/pointclouds/
site/clients/WJ Properties/7 St Dennis - South Garage Slab/stations/
site/clients/WJ Properties/7 St Dennis - South Garage Slab/models/
site/clients/WJ Properties/7 St Dennis - South Garage Slab/*.glb
site/clients/WJ Properties/7 St Dennis - Lounge/HighResolution/
site/clients/WJ Properties/7 St Dennis - Lounge/pointclouds/
site/clients/WJ Properties/7 St Dennis - Lounge/stations/
site/clients/WJ Properties/140 Carlton/pointclouds/
site/clients/WJ Properties/140 Carlton - Unit 2304/pointclouds/
site/clients/WJ Properties/140 Carlton - Unit 2304/stations/
site/clients/WJ Properties/140 Carlton - Unit 2304/HighResolution/
site/clients/WJ Properties/140 Carlton - Unit 2304/models/
site/clients/WJ Properties/140 Carlton - Unit 2304/*.glb
site/clients/WJ Properties/140 Carlton - wall/
# 7 St Dennis - Partial Slab Replacement: octree + panoramas served from R2, raw scan local-only
site/clients/WJ Properties/7 St Dennis - Partial Slab Replacement/pointclouds/
site/clients/WJ Properties/7 St Dennis - Partial Slab Replacement/stations/
site/clients/WJ Properties/7 St Dennis - Partial Slab Replacement/HighResolution/
site/clients/WJ Properties/7 St Dennis - Partial Slab Replacement/*.scan
site/clients/WJ Properties/2560 kingston rd/pointclouds/
site/clients/Solar Restoration/pointclouds/
site/clients/HRI/40 Larch 1/pointclouds/
site/clients/HRI/40 Larch 1/stations/
site/clients/HRI/40 Larch 1/models/
site/clients/HRI/30 Weston Rd/pointclouds/
site/clients/HRI/30 Weston Rd/stations/

# ---------- BDA St. Joseph's Hamilton: octree + panoramas served from Cloudflare R2 ----------
# pointclouds/ + stations/ live in R2 bucket 'enable-pointclouds' (bda/stjoseph/...).
# HighResolution/ is the raw panorama staging dump (served copies in stations/panoramas/).
site/clients/BDA/St. Joseph's Hamilton/pointclouds/
site/clients/BDA/St. Joseph's Hamilton/stations/
site/clients/BDA/St. Joseph's Hamilton/HighResolution/
site/clients/BDA/222 Jarvis Elevator 4/pointclouds/
site/clients/BDA/222 Jarvis Elevator 4/stations/

# ---------- Pomerleau Hart House: octree + panoramas served from Cloudflare R2 (2026-07-06) ----------
# pointclouds/ + stations/ live in R2 bucket 'enable-pointclouds' (pomerleau/harthouse/...).
# "Hart House/" holds the raw LAS/scan + HighResolution panorama staging (not served).
site/clients/Pomerleau/pointclouds/
site/clients/Pomerleau/stations/
site/clients/Pomerleau/Hart House/
# "Shoring" GLB model served from R2 (pomerleau/harthouse/models/shoring.glb + models.json) — 2026-07-13.
site/clients/Pomerleau/*.glb

# ---------- MurrayFranks 1531 Dupont: octree + panoramas + Tekla model served from R2 (2026-07-06) ----------
# All data (pointclouds/stations/models) now in R2 bucket 'enable-pointclouds'
# (murrayfranks/1531dupont/...); projects.js folder points at the r2.dev URL.
# MurrayFranks also went no-login (latest HRI stack) at this point.
site/clients/MurrayFranks/1531 Dupont (steel canopy)/

# ---------- SECRETS — never commit ----------
# Cloudflare R2 upload credentials (Object Read & Write). Plaintext on this machine
# only; NEVER pushed. Agents read this file for R2 uploads. See reference_r2_migration.
.r2creds.json
.r2creds*

# St. Joseph's Hamilton model (served from R2, not git)
site/clients/BDA/St. Joseph's Hamilton/models/
