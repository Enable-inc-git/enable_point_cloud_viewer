# Pretty project URLs:
/clients/:client/:project     /clients/:client/viewer.html?p=:project    200

# Pretty client home:
/clients/:client              /clients/:client/index.html                200

# Allow direct access to the real files (no rewrite):
/clients/:client/index.html   /clients/:client/index.html                200
/clients/:client/viewer.html  /clients/:client/viewer.html               200
